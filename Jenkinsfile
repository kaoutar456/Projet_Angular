pipeline {
    agent any

    stages {
        // Étape 1 : Cloner le projet
        stage('Cloner le projet') {
            steps {
                git 'https://github.com/kaoutar456/Projet_Angular.git'
            }
        }
}
}